server:
  port: 8762
spring:
  application:
    name: nativeconfigserver
  profiles:
    active: native #标识配置文件的获取方式
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared #配置文件的存放路径